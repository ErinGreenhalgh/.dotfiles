# erin: ~/something|gitbranch-name =>
# erin: ~/something|gitbranch-name (icon indicating uncommitted changes) =>

parse_git_branch() {
    git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/ (\1)/'
}

P_CYAN=35

export PS1="\e[35merin:\e[0m \w |$(parse_git_branch)  => "
